version: '3.8'

services:
  # Application .NET avec SVXLink 19.09.2
  svxlinkmanager-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: svxlinkmanager-app
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__PostgreSQL=Host=postgresql;Database=svxlinkmanager;Username=svxlink;Password=${POSTGRES_PASSWORD:-changeme}
    volumes:
      - ./svxlink-config:/etc/svxlink:ro
      - ./logs:/app/logs
      - svxlink-spool:/var/spool/svxlink
      - svxlink-logs:/var/log/svxlink
    depends_on:
      postgresql:
        condition: service_healthy
    networks:
      - svxlink-network
    restart: unless-stopped

  # PostgreSQL Database
  postgresql:
    image: postgres:16-alpine
    container_name: svxlinkmanager-postgresql
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-svxlinkmanager}
      - POSTGRES_USER=${POSTGRES_USER:-svxlink}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-changeme}
      - PGDATA=/var/lib/postgresql/data/pgdata
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U svxlink -d svxlinkmanager"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - svxlink-network
    restart: unless-stopped

networks:
  svxlink-network:
    driver: bridge

volumes:
  postgres-data:
    driver: local
  svxlink-spool:
    driver: local
  svxlink-logs:
    driver: local
